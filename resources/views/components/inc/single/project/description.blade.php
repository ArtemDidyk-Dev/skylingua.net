<div class="single-project-description">
    <x-inc.single.project.user  created="{{ $created }}" photo="{{ $photo }}" name="{{ $name }}" ratingCount="{{$ratingCount}}"  rating="{{$rating}}"
    />
</div>
