<div class="single-project-description">
    <x-inc.single.freelancer.user  created="{{ $created }}" photo="{{ $photo }}" name="{{ $name }}" ratingCount="{{$ratingCount}}"  rating="{{$rating}}"
    />
</div>
